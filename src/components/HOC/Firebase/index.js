import FirebaseContext, { withFirebase } from 'components/HOC/Firebase/context';
import Firebase from 'components/HOC/Firebase/firebase';

export default Firebase;

export { FirebaseContext, withFirebase };